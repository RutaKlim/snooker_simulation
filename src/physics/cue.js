// this should initiate the initial speed and angle, but first we will start with just the angle
export class Cue {
	_curX;
	_curY;
	_startAngle;
	constructor(startX, startY, startAngle) {}
}

// implement being able to use up and down keys to move the cue stick and then back and forward keys for power??
// but id prefer the mouse, then how far it is can be the hypotenuse, and the angle the radian direction
